exports.getKeywords = (req, res) => {
  // Lógica para obter palavras-chave
  res.json({ message: "Obter palavras-chave" });
};

exports.createKeyword = (req, res) => {
  // Lógica para criar uma palavra-chave
  res.json({ message: "Criar palavra-chave" });
};

exports.updateKeyword = (req, res) => {
  // Lógica para atualizar uma palavra-chave
  res.json({ message: "Atualizar palavra-chave" });
};

exports.deleteKeyword = (req, res) => {
  // Lógica para deletar uma palavra-chave
  res.json({ message: "Deletar palavra-chave" });
};
